import { PokemonTypeColourPipe } from './pokemon-type-colour.pipe';

describe('PokemonTypeColourPipe', () => {
  it('create an instance', () => {
    const pipe = new PokemonTypeColourPipe();
    expect(pipe).toBeTruthy();
  });
});
